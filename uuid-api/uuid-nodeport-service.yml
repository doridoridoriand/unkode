---
apiVersion: v1
kind: Service
metadata:
  name: uuid-api-nodeport
  namespace: doridoridoriand
spec:
  type: NodePort
  ports:
    - name: "http-port"
      protocol: "TCP"
      port: 13000
      targetPort: 3000
  selector:
    app: uuid-api-container
